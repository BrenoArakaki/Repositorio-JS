var base, altura, total;
base = parseInt(window.prompt("Qual é a base do triângulo?"));
altura = parseInt(window.prompt("Qual é a altura do triângulo?"));

total = base * altura / 2

document.write("<h1>A área total do triângulo é de: " + total);