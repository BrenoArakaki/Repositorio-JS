var base, altura, total;
base = parseInt(window.prompt("Qual é o tamanho da base?"));
altura = parseInt(window.prompt("Qual é a altura"));

total = base * altura

document.write("<h1>A área total do retangulo é de: " + total);