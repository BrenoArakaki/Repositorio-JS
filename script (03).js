var diagonal, total;
      diagonal = parseInt(window.prompt("Qual é a diagonal do quadrado?"));

      total = diagonal * diagonal / 2

      document.write("<h1>A área total do quadrado é de: " + total);